package com.anshumanprajapati;

import java.util.*;

public class Main {
    public static void main(String[] args) {
            /*

            t1, t2
            Atomic i =  1;

            String evenLock;
            String oddLock;

            t1 -> {
                sync(evenLock){
                    sout(i++)
                }

            }
            t2 -> {
                sync(oddlock){
                    sout(i++)
                }
            }


             */

        /*

        server
        - client A -> micro B -> server C
        - Client C -> micro B -> Server A

        1 /endpoint1 -> B -> RestClient C
        2 /endpoint1 -> B -> RestClient A

        -> LOW LEVEL OPTIMISATION
            -
        -> HIGH LEVEL

        - A1 -> ms1 -> S C
        - A2 -> ms1 -> S C


        - openapispec
        - microservice
        - git
        - CI
        - build
        - test (junit)
        - Dockerfile  (mvn build) Image -> docker run
        - docker repo google
        - values.yaml (key,values)
        - application-dev, prod.yml

        - CD
        -



         */
    }
}